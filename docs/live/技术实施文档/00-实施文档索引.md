# MambaResearch 插件化改造：技术实施文档索引

状态：实施前基线
适用分支：<code>development</code>
最后更新：2026-08-15

## 1. 文档目的

本目录把上层架构蓝图拆成可直接实施和验收的技术合同。当前只覆盖第一条纵向切片：

~~~text
Mamba Kernel
  -> Capability Registry
  -> Backend Plugins
  -> Terminal / Auth / Conversation consumers
~~~

先验证“新增 backend 不再修改核心终端流程”这一条真实扩展需求。Research Plugins 和 UI Plugins
等到该切片稳定后再写实施文档，避免提前设计尚未验证的扩展框架。

上层设计见
[Mamba Kernel 插件化架构](../架构设计/mamba-kernel-plugin-architecture.md)。

## 2. 文档清单

| 文档 | 解决的问题 | 实施阶段 |
| --- | --- | --- |
| [01-Mamba-Kernel-技术设计](01-Mamba-Kernel-技术设计.md) | 插件如何被选择、校验、注册、启动和释放 | Batch 1 |
| [02-Backend-Registry-技术设计](02-Backend-Registry-技术设计.md) | Codex 如何先成为首个可注册 backend，并由 API、前端和存储共同消费 | Batch 2 |
| [03-测试规范](03-测试规范.md) | 单元、集成和系统测试如何归属、组织、执行与裁剪 | 所有 Batch |

## 3. 已冻结决策

1. Mamba Kernel 是薄宿主和装配器，不是 Agent Runtime。
2. Codex CLI 的本地 transcript 继续是真相源；SQLite 只保存编排元数据和镜像。
3. 第一阶段插件均为仓库内受信任 Python 模块，由显式 catalog 提供，不做目录扫描和动态下载。
4. Registry 只提供有类型的能力集合，不提供任意字符串到任意对象的 service locator。
5. <code>create_app()</code> 只完成配置读取、依赖校验、纯注册和路由挂载；进程资源只在
   FastAPI lifespan 内启动。
6. 启动失败立即失败，并逆序释放已经启动的资源；不保留旧路径、fallback、feature flag 或双路由。
7. 前端通过 <code>GET /api/capabilities</code> 获取 backend inventory，不再维护
   <code>'claude' | 'codex'</code> 联合类型。
8. 数据库中的 backend/source 字段保存稳定字符串 ID，不再使用固定枚举
   <code>CHECK</code>。
9. 第一批只实现并启用 <code>backend.codex</code>；Claude backend 插件在 Codex 纵向切片
   验收后单独补充。
10. Backend seam 切换后不保留 Claude 旧终端路径；历史 Claude 元数据可读，但不能新建或
    resume，直到 Claude 插件完成。
11. 旧测试基线清零；后续测试统一按功能镜像树组织，并遵守
    [测试规范](03-测试规范.md)。

## 4. 明确不做

- 不引入 Cordis 依赖。
- 不实现远程插件、插件市场、包签名、权限沙箱或热重载。
- 不实现运行时远程 UI 代码。
- 不引入通用 event bus、通用 DI 容器或通用 hook 系统。
- 不重写 PTY bridge、CLI transcript、MCP 协议或现有业务模块。
- 不同时迁移 Research/UI 插件。

这些能力只有在出现对应的真实使用场景后，才单独立项。

## 5. 实施批次

### Batch 1：Kernel 宿主

实施 [01-Mamba-Kernel-技术设计](01-Mamba-Kernel-技术设计.md)：

1. 新增最小 Kernel 合同、typed registry、profile loader 和 lifecycle。
2. 建立仓库内 built-in plugin catalog。
3. 将应用装配改为 <code>create_app()</code> + lifespan。
4. 用现有路由的薄插件完成注册，不移动业务代码。
5. 验证重复 ID、缺失依赖、依赖环和失败回滚。

Batch 1 结束时，应用行为不新增功能；Kernel 只是替代散落在
<code>app.py</code> 中的装配职责。

### Batch 2：Backend seam

实施 [02-Backend-Registry-技术设计](02-Backend-Registry-技术设计.md)：

1. 定义 <code>TerminalBackend</code>，先封装 Codex 启动逻辑。
2. 终端路由按 registry 查找 backend，删除所有名称分支和未插件化的 Claude 路径。
3. 新增 capability inventory，认证探测改为 backend 驱动。
4. 前端从 inventory 选择 backend。
5. 追加 SQLite migration，解除固定 backend/source 枚举。

Batch 2 结束时，增加一个测试 backend 只需要实现并注册 provider，不修改终端路由、
认证聚合器和前端 backend 类型；下一批 Claude 插件也使用同一接入方式。

## 6. 阶段门禁

| 门禁 | 必须满足 |
| --- | --- |
| G1：合同冻结 | 两份技术设计中的 public contract、错误语义和 API schema 无未决项 |
| G2：Kernel 完成 | loader/lifecycle 单元检查通过，现有 HTTP 路由仍可用 |
| G3：Backend 完成 | Codex 新建与 resume 主路径通过，未知/未启用的 Claude backend 明确失败 |
| G4：数据完成 | v1-v6 数据升级后行数和值保持不变，新 backend ID 可写入和读取 |
| G5：删除旧路径 | <code>SUPPORTED_BACKENDS</code>、backend 名称分支和固定前端联合类型已删除 |

未通过前一门禁，不开始后一批次。Research/UI 技术设计在 G5 后再建立。

## 7. 完成定义

当前文档计划完成，不代表代码改造完成。代码改造的完成定义为：

- 两个 Batch 的验收矩阵全部通过；
- 每个非平凡原子功能有最小单元测试，跨模块业务编排有集成测试；
- 旧装配与旧 backend 分支被删除，而不是保留为兼容路径；
- 当前 Codex PTY、resume、会话镜像和认证状态行为保持；
- 新 backend 的接入改动局限于一个插件模块和 profile/catalog 注册；
- 没有新增本阶段范围外的插件基础设施。

Codex-only 切片通过后，下一份实施文档只描述 Claude adapter 的本地接入，不重开 Kernel、
terminal、auth、frontend 或 schema 设计。

## 8. 文档维护规则

- 上层“为什么这样设计”写入架构设计；本目录只记录“具体怎样实现和验收”。
- 合同变更必须先更新对应技术设计，再进入代码批次。
- 实施中发现与当前代码不符的事实，直接修正文档，不新增兼容层掩盖差异。
- 文档定稿前保留在本地；当前仓库的 <code>.git/info/exclude</code> 已忽略
  <code>docs/</code>，准备提交时需显式处理该本地规则。
