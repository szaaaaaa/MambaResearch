"""Model registry placeholder for numpy_minimal template.

The default train.py uses inline linear regression — no model class needed.
Agents that introduce more complex models can import or register them here.
"""
