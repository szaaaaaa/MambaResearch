[pytest]
pythonpath = .
testpaths = tests
norecursedirs = data build .tmp .tmp_* tmp* .pytest_cache
markers =
    integration: integration tests requiring real external services (default: skipped)
addopts = -m "not integration"
